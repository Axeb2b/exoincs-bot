{
  "name": "exoincs-bot",
  "version": "1.0.0",
  "description": "Telegram bot + API for per-domain wallet config and cloaking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "telegraf": "^4.16.3"
  }
}
